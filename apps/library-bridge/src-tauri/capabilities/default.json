{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Minimal capability surface for the Ensemblis Library Bridge webview.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
