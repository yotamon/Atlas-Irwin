import { createClient } from "@supabase/supabase-js";
if(process.env.NODE_ENV==="production"||process.env.ALLOW_STUDIO_DEMO_SEED!=="true")throw new Error("Demo seed is disabled. Set ALLOW_STUDIO_DEMO_SEED=true outside production.");
const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY,email=(process.env.STUDIO_IMPORT_ADMIN_EMAIL||process.env.STUDIO_ADMIN_EMAILS?.split(",")[0]||"").trim().toLowerCase();if(!url||!key||!email)throw new Error("Missing Supabase/admin environment variables.");const supabase=createClient(url,key,{auth:{persistSession:false}});const {data:profile}=await supabase.from("profiles").select("id").eq("email",email).single();if(!profile)throw new Error("Admin profile not found.");const {data:release,error}=await supabase.from("releases").upsert({owner_id:profile.id,title:"Studio Signal",slug:"studio-signal-demo",release_type:"Single",status:"In Progress",core_emotion:"the electricity of a room beginning to move",audience:"late-night disco listeners",primary_hook:"a chrome-bright bass turn",visual_direction:"warm silhouettes and chrome reflections"},{onConflict:"owner_id,slug"}).select("id").single();if(error)throw error;await supabase.from("tasks").insert({owner_id:profile.id,release_id:release.id,title:"Shape the release story",priority:"High"});console.log("Local Studio demo seed complete.");
