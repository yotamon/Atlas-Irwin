# DJ & Mixes Set Intelligence release scope

The merge candidate includes Set Intelligence, bounded Personal DJ Intelligence and verified transition previews on top of the existing professional AutoMix engine.

The release intentionally stops before external DJ-library adapters and desktop/device execution. Those phases must reuse `ensemblis.automix-source.v1`, Set Intent and MixPlan v2 rather than introducing parallel planning or rendering paths.
